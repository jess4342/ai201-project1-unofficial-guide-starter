sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5):
gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — used to inspect documents/thebravern.pdf (image-only, excluded):
pdfplumber>=0.11.4

# Stretch feature — hybrid search (BM25 lexical scoring):
rank-bm25==0.2.2
